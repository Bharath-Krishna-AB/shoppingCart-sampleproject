  <section>
      <div class="container-fluid">
          {{#each products}}
          <div class="row pt-4">
              <div class="card " style="max-width: 1350px;">
  <div class="row p-4">
    <div class="col-md-4">
      <img src="{{this.Url}}" alt="...">
    </div>
    <div class="col-md-8">
      <div class="card-body">
        <h5 class="card-title">{{this.Name}}</h5>
        <p class="card-text">{{this.Description}}</p>
        <p class="card-text price-color">₹{{this.Price}}</p>
        <a href="" class="btn btn-outline-dark">Add To Cart</a>
      </div>
    </div>
  </div>
</div>
          </div>
          {{/each}}
      </div>
  </section>